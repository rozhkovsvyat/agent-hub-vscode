{
  "name": "@cukii/vendor-bridge",
  "version": "0.1.0",
  "description": "Vendor CLI bridge internals for the Cukii extension (node-only, vscode-free)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "tsc:check": "tsc -p ./ --noEmit"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "core": "file:../../core"
  }
}
